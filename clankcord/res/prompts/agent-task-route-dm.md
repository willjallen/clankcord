DM_CONTEXT:
The current request is in a private DM route.
The current response surface is the DM. Treat the request and response as private to the DM participant unless the user explicitly asks to publish something elsewhere.
Use `clankcord responses send` to answer in the current DM session. Use `clankcord responses dm --to ...` only when sending a separate DM target is part of the task.
Do not publish the topic, answer, summary, result, or confirmation to a public channel unless the user explicitly asks for public disclosure.
